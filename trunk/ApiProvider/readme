生成的makefile需要修改一下：
INCPATH加入 -I../ApiProvider/include

test编译命令是
g++ -I$QT头文件目录 -I$代码目录/ApiProvider/include  -L$生成so目录 -lApiProvider  test.cpp -o test -g


ApiProvider库本次合入内容：

api基本框架
部分参数定义
一个api接口函数

TODO LIST：
1.尚未调用httphelper
2.返回值xml处理尚未考虑
3.返回的list类，如status类，user类

聂耀龙20110607
